// Default API host (OCI). Local override: npm run ios-pos:local-config → Config/Local.xcconfig
API_BASE_URL = https:/$()/oci.cloudstore893.com/
POS_SALES_FEE_RATE = 0.0
POS_TAX_RATE = 0.06
#include? "Local.xcconfig"
